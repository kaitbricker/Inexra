import { Integration } from '@sentry/types';
export interface Options {
    /**
     * Whether breadcrumbs should be recorded for requests
     * Defaults to true
     */
    breadcrumbs: boolean;
    /**
     * Function determining whether or not to create spans to track outgoing requests to the given URL.
     * By default, spans will be created for all outgoing requests.
     */
    shouldCreateSpanForRequest?: (url: string) => boolean;
}
/**
 * Creates spans and attaches tracing headers to fetch requests on WinterCG runtimes.
 */
export declare class WinterCGFetch implements Integration {
    /**
     * @inheritDoc
     */
    static id: string;
    /**
     * @inheritDoc
     */
    name: string;
    private readonly _options;
    private readonly _createSpanUrlMap;
    private readonly _headersUrlMap;
    constructor(_options?: Partial<Options>);
    /**
     * @inheritDoc
     */
    setupOnce(): void;
    /** Decides whether to attach trace data to the outgoing fetch request */
    private _shouldAttachTraceData;
    /** Helper that wraps shouldCreateSpanForRequest option */
    private _shouldCreateSpan;
}
//# sourceMappingURL=wintercg-fetch.d.ts.map
