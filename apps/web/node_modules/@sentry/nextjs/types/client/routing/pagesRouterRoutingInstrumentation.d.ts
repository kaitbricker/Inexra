import type { Transaction, TransactionContext } from '@sentry/types';
type StartTransactionCb = (context: TransactionContext) => Transaction | undefined;
/**
 * Instruments the Next.js pages router. Only supported for
 * client side routing. Works for Next >= 10.
 *
 * Leverages the SingletonRouter from the `next/router` to
 * generate pageload/navigation transactions and parameterize
 * transaction names.
 */
export declare function pagesRouterInstrumentation(startTransactionCb: StartTransactionCb, startTransactionOnPageLoad?: boolean, startTransactionOnLocationChange?: boolean): void;
export {};
//# sourceMappingURL=pagesRouterRoutingInstrumentation.d.ts.map